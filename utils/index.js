class Utils {
  
  /**
   * parserData
   */
  static parserDataTestMode(type) {
    const fileName = type === 'day'
      ? 'demoPumpDayData.csv'
      : 'demoCompressorWeekData.csv';
    
      
  }
}

module.exports = Utils;
