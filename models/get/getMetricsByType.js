/**
 * 
 * @param {*} type 
 * @returns [Object]
 */
const getMetricsByType = (type="day") => new Promise((resolve, reject) =>{
  console.log(type);
  resolve('');
});

module.exports = getMetricsByType;
